#pragma once

// The static HTML/JS for the config page. Kept in its own translation unit
// so editing the front-end never touches the HTTP engine or route handlers.
extern const char WEB_CONFIG_HTML[];
